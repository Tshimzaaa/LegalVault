Good, that's the hard infrastructure part done. Now it's on his end mostly, but here's exactly what needs to happen to wire up a login form to your API.

## What he needs to build (React + Vite)

### 1. A login function

```javascript
// api/auth.js
const BASE_URL = "http://127.0.0.1:8000";

export async function login(email, password) {
  const res = await fetch(`${BASE_URL}/auth/login`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ email, password }),
  });

  if (!res.ok) {
    const errorData = await res.json();
    throw new Error(errorData.detail || "Login failed");
  }

  const data = await res.json();
  return data.access_token;
}

export async function getCurrentUser(token) {
  const res = await fetch(`${BASE_URL}/auth/me`, {
    headers: { Authorization: `Bearer ${token}` },
  });

  if (!res.ok) {
    throw new Error("Not authenticated");
  }

  return res.json();
}
```

### 2. A login form component

```jsx
import { useState } from "react";
import { login, getCurrentUser } from "../api/auth";

function LoginForm({ onLoginSuccess }) {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");

  async function handleSubmit(e) {
    e.preventDefault();
    setError("");

    try {
      const token = localStorage.getItem("access_token") || (await login(email, password));
      localStorage.setItem("access_token", token);

      const user = await getCurrentUser(token);
      onLoginSuccess(user);
    } catch (err) {
      setError(err.message);
    }
  }

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="email"
        placeholder="Email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        required
      />
      <input
        type="password"
        placeholder="Password"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
        required
      />
      {error && <p style={{ color: "red" }}>{error}</p>}
      <button type="submit">Log In</button>
    </form>
  );
}

export default LoginForm;
```

(Small bug avoidance note for him: don't actually check `localStorage` before calling `login()` like I half-wrote above — that was leftover logic that doesn't belong there. Just call `await login(email, password)` directly. I'll clean that up below.)

Corrected line in `handleSubmit`:
```javascript
const token = await login(email, password);
localStorage.setItem("access_token", token);
```

### 3. Keeping the user logged in across page refreshes

```jsx
import { useState, useEffect } from "react";
import { getCurrentUser } from "../api/auth";

function App() {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const token = localStorage.getItem("access_token");
    if (token) {
      getCurrentUser(token)
        .then(setUser)
        .catch(() => localStorage.removeItem("access_token"))
        .finally(() => setLoading(false));
    } else {
      setLoading(false);
    }
  }, []);

  if (loading) return <p>Loading...</p>;

  return user ? <Dashboard user={user} /> : <LoginForm onLoginSuccess={setUser} />;
}
```

### 4. Logout

```javascript
function logout() {
  localStorage.removeItem("access_token");
  window.location.reload();
}
```

---

## What he should test on his end

1. Type wrong password → form should show the error message from your API
2. Type correct credentials → should log in and load `/auth/me` data
3. Refresh the page → should stay logged in (via the `useEffect` check)
4. Click logout → should clear token and show login form again

Have him try it against the test account you already made (`test@legalhub.com` / `Password123!`) since that user already exists in your shared database. Let me know what breaks, if anything, once he wires it up.